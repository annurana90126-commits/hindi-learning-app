import 'package:flutter/material.dart';

class AppColors {
  // Primary - Saffron (Indian flag inspired)
  static const Color primary = Color(0xFFFF6B35);
  static const Color primaryDark = Color(0xFFE55A2B);
  static const Color primaryLight = Color(0xFFFF8C5A);

  // Secondary - Deep Blue
  static const Color secondary = Color(0xFF1A237E);
  static const Color secondaryLight = Color(0xFF3949AB);

  // Accent - Gold
  static const Color accent = Color(0xFFFFD700);

  // Success / Error / Warning
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA726);

  // Neutrals
  static const Color background = Color(0xFFFAF8F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFF5F0EB);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B6B8A);
  static const Color textHint = Color(0xFFB0B0C8);

  // XP / Gamification
  static const Color xpGold = Color(0xFFFFD700);
  static const Color streakOrange = Color(0xFFFF6B35);
  static const Color levelPurple = Color(0xFF7B2FBE);
}